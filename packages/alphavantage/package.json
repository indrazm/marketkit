{
  "name": "@marketkit/alphavantage",
  "version": "0.0.0",
  "files": [
    "dist"
  ],
  "type": "module",
  "sideEffects": false,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "test": "pnpm -r build && node test/smoke.mjs",
    "check": "tsc -p tsconfig.json && tsc --noEmit --strict --target es2022 --module nodenext --moduleResolution nodenext --ignoreConfig test/types.typecheck.ts"
  },
  "dependencies": {
    "@marketkit/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
